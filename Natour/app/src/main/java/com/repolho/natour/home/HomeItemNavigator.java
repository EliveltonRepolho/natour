package com.repolho.natour.home;

/**
 * Defines the navigation actions that can be called from a list item in the task list.
 */
public interface HomeItemNavigator {

    void openPointDetails(String taskId);
}
